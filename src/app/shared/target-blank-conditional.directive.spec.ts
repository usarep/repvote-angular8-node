import { TargetBlankConditionalDirective } from './target-blank-conditional.directive';

describe('TargetBlankConditionalDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetBlankConditionalDirective();
    expect(directive).toBeTruthy();
  });
});
